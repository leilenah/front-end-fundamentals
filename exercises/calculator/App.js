import React from "react";
import { Calculator } from "./Calculator";
import "./styles.css";

/**
 * Build a calculator application
 *
 * UI
 *  numbers
 *  decimal point
 *  X, %, +, -
 *  =
 *
 * Display the result when user hits =
 * Indicate when the user has clicked a number or symbol
 */

export default function App() {
  return <Calculator />;
}
